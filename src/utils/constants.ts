/**
 * Step used for computing positions when inserting items/lists (e.g. drag-and-drop).
 * In future, a rebalance (normalization) of positions may be needed after many reorders.
 */
export const POSITION_GAP = 1024;
